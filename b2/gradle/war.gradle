apply plugin: "war"

bootWar {
    mainClass = "edu.uanfilms.moviereview.GatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
