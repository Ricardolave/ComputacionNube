apply plugin: "war"

bootWar {
    mainClass = "edu.uanfilms.moviereview.UanfilmsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
